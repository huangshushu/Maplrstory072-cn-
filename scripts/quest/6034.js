var status = -1;

function start(mode, type, selection) {
}

function end(mode, type, selection) {
        qm.forceCompleteQuest(6034);
        qm.forceStartQuest(6035);
        qm.gainItem(4031979, 1);
        qm.dispose();
}
